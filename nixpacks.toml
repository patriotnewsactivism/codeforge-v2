[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = [
  "npx convex codegen",
  "npx vite build"
]

[start]
cmd = "npx serve dist -s -l $PORT"

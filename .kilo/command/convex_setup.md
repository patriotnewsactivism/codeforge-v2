Convex Setup Guide
- Install convex package: npm i convex
- Create a convex client with createConvexClient(address)
- Use client.mutations and client.queries to persist and query knowledgeBase and templates
- Point to convex.cloud in production, or localhost:8383 for local dev
